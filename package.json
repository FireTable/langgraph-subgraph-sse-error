{
  "name": "langgraph-subgraph-sse-error",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "Minimal repro for the LangGraph.js EventStreamCallbackHandler run-map bug when a compiled subgraph is used as a node in a parent graph and the parent streams events.",
  "scripts": {
    "dev": "rm -f log.txt && concurrently -n frontend,backend -c cyan,magenta  \"pnpm dev:backend\" \"pnpm dev:client\" 2>&1 | tee log.txt",
    "dev:backend": "langgraphjs dev  --port 3938",
    "dev:client": "LANGGRAPH_API_URL=http://localhost:3938 tsx run-client.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@langchain/core": "^1.2.1",
    "@langchain/langgraph": "^1.4.7",
    "@langchain/langgraph-checkpoint": "^1.1.3",
    "@langchain/openai": "^1.5.3",
    "dotenv": "^16.6.1",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@langchain/langgraph-cli": "^1.4.1",
    "@types/node": "^22.20.0",
    "concurrently": "^10.0.3",
    "tsx": "^4.22.4"
  },
  "packageManager": "pnpm@10.34.4",
  "engines": {
    "node": ">=22"
  }
}